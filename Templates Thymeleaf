<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>Lista de Livros</title>
</head>
<body>
<h1>Biblioteca - Lista de Livros</h1>

<a href="/livros/novo">+ Novo Livro</a>
<table border="1" cellpadding="5">
    <tr>
        <th>ID</th><th>Título</th><th>Autor</th><th>Ano</th><th>Gênero</th><th>Ações</th>
    </tr>
    <tr th:each="livro : ${livros}">
        <td th:text="${livro.id}"></td>
        <td th:text="${livro.titulo}"></td>
        <td th:text="${livro.autor}"></td>
        <td th:text="${livro.ano}"></td>
        <td th:text="${livro.genero}"></td>
        <td>
            <a th:href="@{'/livros/ver/' + ${livro.id}}">Ver</a> |
            <a th:href="@{'/livros/editar/' + ${livro.id}}">Editar</a> |
            <a th:href="@{'/livros/remover/' + ${livro.id}}">Remover</a>
        </td>
    </tr>
</table>
</body>
</html>

----------------------------------------------------
<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>Cadastro de Livro</title>
</head>
<body>
<h1 th:text="${livro.id == null ? 'Novo Livro' : 'Editar Livro'}"></h1>

<form action="/livros/salvar" method="post">
    <input type="hidden" th:field="*{id}" />
    <label>Título:</label><input type="text" th:field="*{titulo}" /><br/>
    <label>Autor:</label><input type="text" th:field="*{autor}" /><br/>
    <label>Ano:</label><input type="number" th:field="*{ano}" /><br/>
    <label>Gênero:</label><input type="text" th:field="*{genero}" /><br/>
    <button type="submit">Salvar</button>
</form>

<a href="/livros">Voltar</a>
</body>
</html>

----------------------------------------------------
<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>Visualizar Livro</title>
</head>
<body>
<h1 th:text="${livro.titulo}"></h1>

<p><strong>Autor:</strong> <span th:text="${livro.autor}"></span></p>
<p><strong>Ano:</strong> <span th:text="${livro.ano}"></span></p>
<p><strong>Gênero:</strong> <span th:text="${livro.genero}"></span></p>

<a href="/livros">Voltar</a>
</body>
</html>
